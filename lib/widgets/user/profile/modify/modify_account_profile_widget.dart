import 'package:flutter/cupertino.dart';

class ModifyAccountProfile extends StatelessWidget {
  const ModifyAccountProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("modify"),
    );
  }
}
